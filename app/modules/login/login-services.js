'use strict';

angular.module('login')

    .factory('LoginService', [
        function() {

            var service = {};

            service.login = function(username, password, callback) {

            };

            return service;
        }]);

